services:
  api-1: &api-service
    container_name: api-1
    build:
      context: .
      target: development
    env_file:
      - .env
    environment:
      - SERVER_SOCKET_PATH=/tmp/api_1.sock
    volumes:
      - .:/app
      - socket_data:/tmp
    command: ['bun', 'run', 'dev']
    networks:
      - backend
      - payment-processor
    depends_on:
      - database
    deploy:
      resources:
        limits:
          cpus: '0.675'
          memory: '135MB'

  api-2:
    <<: *api-service
    container_name: api-2
    environment:
      - SERVER_SOCKET_PATH=/tmp/api_2.sock

  database:
    container_name: database
    build:
      context: .
      dockerfile: Dockerfile.database
      target: development
    env_file:
      - .env
    volumes:
      - .:/app
      - socket_data:/tmp
    command: ['bun', '--watch', 'run', 'src/db-server/index.ts']
    deploy:
      resources:
        limits:
          cpus: '0.05'
          memory: '15MB'

  load-balancer:
    container_name: load-balancer
    build:
      context: ./load-balancer
      target: production
    restart: unless-stopped
    volumes:
      - socket_data:/tmp
    networks:
      - backend
    ports:
      - '9999:80'
    depends_on:
      - api-1
      - api-2
    deploy:
      resources:
        limits:
          cpus: '0.1'
          # memory: '65MB'

networks:
  backend:
    driver: bridge
  payment-processor:
    external: true

volumes:
  socket_data:
